# 급식 배틀 - 학교 급식 조회 앱 PRD

## 1. 문서 목적

이 문서는 NEIS 교육정보 개방 포털의 공개 API를 이용해 학교별 중식 정보를
조회하는 웹 애플리케이션의 제품 요구사항을 정의한다. 제품 범위와 사용자 경험에
대한 단일 기준으로 사용하며, 구체적인 구현 방식은 `TRD.md`에서 정의한다.

## 2. 개발 목적

사용자가 학교 이름과 날짜 범위만으로 원하는 학교의 날짜별 중식 메뉴를 쉽고
빠르게 확인할 수 있도록 한다. NEIS API의 코드나 응답 구조를 사용자가 알 필요
없이 학교 검색부터 급식 확인까지 일관된 흐름을 제공한다.

## 3. 대상 사용자

- 특정 학교의 급식 일정을 확인하려는 학생과 보호자
- 학교 급식 정보를 안내하거나 확인해야 하는 교직원
- 여러 날짜의 중식 메뉴를 한 화면에서 살펴보려는 일반 사용자

## 4. 제품 범위

### 4.1 포함 범위

- 학교 이름 일부를 이용한 학교 검색
- 검색 결과에서 한 학교 선택
- 시작일과 종료일을 이용한 날짜 범위 선택
- 선택한 학교의 중식 기준 날짜별 급식 정보 조회
- 조회 진행, 빈 결과, 입력 오류, 외부 서비스 오류 상태 안내
- 데스크톱과 모바일 웹 환경에서 사용할 수 있는 반응형 UI

### 4.2 제외 범위

- 조식 및 석식 조회
- 로그인, 사용자 계정 및 즐겨찾기
- 급식 정보 수정 또는 등록
- 학교 간 급식 비교, 점수 산정 및 AI 분석
- 조회 기록의 영구 저장

## 5. 핵심 사용자 흐름

사용자는 다음 3단계를 순서대로 진행한다.

1. **학교 검색 및 선택**
   - 학교 이름의 일부를 입력해 검색한다.
   - 학교명과 지역 등 식별 정보를 확인하고 한 학교를 선택한다.
2. **날짜 범위 선택**
   - 조회 시작일과 종료일을 선택한다.
   - 시작일이 종료일보다 늦지 않은 유효한 범위를 입력한다.
3. **급식 결과 확인**
   - 선택한 학교와 날짜 범위에 해당하는 중식 정보를 조회한다.
   - 결과를 날짜순으로 확인한다.

학교 또는 날짜를 변경하면 기존 결과가 새 조건의 결과로 오인되지 않도록
초기화하고 다시 조회하게 한다.

## 6. 기능 요구사항

### FR-1. 학교 검색

- 사용자는 학교 이름의 일부를 검색어로 입력할 수 있어야 한다.
- 공백만 있는 검색어는 유효한 검색어로 처리하지 않아야 한다.
- 검색 중임을 알 수 있는 상태를 표시해야 한다.
- 검색 결과에는 최소한 학교명과 사용자가 동명의 학교를 구분할 수 있는 지역
  또는 교육청 정보를 표시해야 한다.
- 검색 결과가 여러 개인 경우 사용자가 한 학교를 명시적으로 선택할 수 있어야
  한다.

### FR-2. 날짜 범위 선택

- 학교를 선택한 후 시작일과 종료일을 입력할 수 있어야 한다.
- 시작일과 종료일은 모두 필수다.
- 시작일은 종료일보다 늦을 수 없다.
- 유효하지 않은 날짜 범위에서는 조회를 실행하지 않고 수정 방법을 안내해야
  한다.

### FR-3. 중식 조회

- 학교와 유효한 날짜 범위가 선택된 경우에만 조회할 수 있어야 한다.
- 조회 대상은 중식으로 제한해야 한다.
- 선택한 시작일과 종료일을 모두 포함해 조회해야 한다.
- 결과는 급식일 기준 오름차순으로 표시해야 한다.
- 각 결과에는 최소한 날짜, 학교명, 식사 구분 및 메뉴를 표시해야 한다.
- NEIS가 제공하는 경우 열량, 영양정보, 원산지정보 및 급식인원수도 표시할 수
  있어야 한다.
- 메뉴 데이터에 포함된 줄바꿈 표기는 사용자가 읽기 쉬운 형태로 표현해야 한다.

### FR-4. 상태 및 오류 안내

- 학교 검색과 급식 조회 각각에 로딩 상태를 표시해야 한다.
- 검색 또는 조회 실패 시 사용자가 이해할 수 있는 오류 메시지와 재시도 방법을
  제공해야 한다.
- 이전 요청의 결과가 진행 중인 새 요청의 결과처럼 표시되지 않아야 한다.

## 7. 예외 상황

| 상황 | 기대 동작 |
|---|---|
| 검색어가 비어 있거나 공백뿐임 | 검색을 실행하지 않고 학교명을 입력하도록 안내한다. |
| 검색 결과가 없음 | 조건에 맞는 학교가 없음을 표시하고 검색어 수정을 안내한다. |
| 동명의 학교가 여러 개임 | 지역 또는 교육청 정보를 함께 표시해 사용자가 직접 선택하게 한다. |
| 학교를 선택하지 않음 | 날짜별 급식 조회를 실행하지 않고 학교 선택을 안내한다. |
| 시작일 또는 종료일이 없음 | 누락된 날짜를 입력하도록 안내한다. |
| 시작일이 종료일보다 늦음 | 잘못된 날짜 범위임을 표시하고 조회를 차단한다. |
| 선택 범위에 중식 정보가 없음 | 오류로 위장하지 않고 해당 기간에 중식 정보가 없음을 표시한다. |
| NEIS 인증 또는 요청 제한 오류 | 서비스 이용이 일시적으로 불가능함을 알리고 적절한 재시도를 안내한다. |
| 네트워크 또는 서버 오류 | 기존 결과와 구분되는 오류 상태를 표시하고 재시도 기능을 제공한다. |

## 8. 비기능 요구사항

### 8.1 사용성 및 접근성

- 키보드만으로 검색, 학교 선택, 날짜 입력 및 조회가 가능해야 한다.
- 입력 필드와 버튼에는 의미를 알 수 있는 레이블을 제공해야 한다.
- 로딩, 오류 및 빈 결과 상태를 색상만으로 구분하지 않아야 한다.
- 주요 화면은 모바일과 데스크톱에서 가로 스크롤 없이 사용할 수 있어야 한다.

### 8.2 보안 및 개인정보

- NEIS API 인증키를 브라우저에 노출하지 않아야 한다.
- 사용자의 검색어와 날짜 입력을 신뢰하지 않고 서버 경계에서 검증해야 한다.
- 이 제품은 개인정보를 요구하거나 저장하지 않아야 한다.

### 8.3 신뢰성

- NEIS가 HTTP 200 응답 안에 업무 오류를 반환할 수 있음을 고려해야 한다.
- 외부 서비스 실패와 정상적인 빈 결과를 구분해 안내해야 한다.
- 외부 API의 원본 응답 구조가 프론트엔드에 직접 노출되지 않아야 한다.

## 9. 완료 기준

- 사용자가 부분 학교명 검색부터 학교 선택, 날짜 범위 선택, 날짜별 중식 결과
  확인까지 3단계 흐름을 완료할 수 있다.
- 잘못된 날짜 범위에서는 외부 조회가 실행되지 않는다.
- 학교 검색 결과 없음과 급식 정보 없음을 각각 명확한 빈 상태로 확인할 수 있다.
- 프론트엔드는 NEIS API를 직접 호출하지 않으며 인증키가 브라우저에 노출되지
  않는다.
- 프론트엔드와 백엔드를 함께 실행하는 표준 개발 절차가 제공된다.

