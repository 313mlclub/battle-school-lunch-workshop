# AGENTS.md

## 프로젝트 개요

이 저장소는 NEIS 공개 API를 활용해 학교와 날짜 범위별 중식 메뉴를 조회하는
웹 애플리케이션과 해당 애플리케이션을 단계적으로 확장하는 워크숍 문서를
포함합니다.

현재 애플리케이션은 다음 구성으로 구현되어 있습니다.

- `src/frontend`: React 19, TypeScript, Vite, Fluent UI 기반 단일 페이지 앱
- `src/backend`: FastAPI, Pydantic, HTTPX 기반 NEIS 프록시 API
- `src/openapi.json`: 프론트엔드와 백엔드 사이의 내부 API 계약
- `data/openapi.json`: NEIS 외부 API 계약
- `compose.yaml`: 프론트엔드와 백엔드를 함께 실행하는 Docker Compose 구성
- `scripts/start.ps1`, `scripts/start.sh`: 로컬 개발용 통합 실행 스크립트
- `docs`: 단계별 워크숍 문서

## 일반 작업 지침

- 요청된 범위와 관련 파일을 먼저 확인하고 변경을 해당 범위로 제한합니다.
- 이 문서보다 구체적인 저장소 문서, 디렉터리별 지침, 설정 파일 및 기존 코드
  패턴이 있으면 이를 우선합니다.
- 기존 동작과 공개 인터페이스를 보존하며, 동작 변경이 필요하면 테스트와
  문서를 함께 갱신합니다.
- 현재 요구사항에 필요하지 않은 리팩터링, 새로운 계층, 범용화 및 추상화를
  추가하지 않습니다.
- 이미 사용 중인 헬퍼와 패턴을 재사용하고 중복 구현을 피합니다.
- 생성 파일이나 외부 명세를 수정할 때는 원본 및 생성 절차를 먼저 확인합니다.

## 애플리케이션 계약 및 구조

- 브라우저는 `/api` 경로로 백엔드만 호출하며 NEIS API를 직접 호출하지 않습니다.
  NEIS 인증키나 엔드포인트를 프론트엔드 번들에 포함하지 않습니다.
- 내부 API의 경로, 요청 및 응답을 변경할 때는 `src/openapi.json`, 백엔드 모델과
  라우트, `src/frontend/src/api.ts`의 타입과 클라이언트를 함께 갱신합니다.
- NEIS 요청 또는 응답 처리를 변경할 때는 `data/openapi.json`을 먼저 확인하고
  문서에 없는 필드나 오류 코드를 추측하지 않습니다.
- 백엔드는 라우트 및 HTTP 매핑(`app/main.py`), 유스케이스(`app/service.py`),
  외부 통신(`app/neis.py`), 내부 및 외부 모델(`app/models.py`)의 책임을
  유지합니다.
- 프론트엔드는 API 호출을 `src/frontend/src/api.ts`에 모으고 컴포넌트에서
  요청 URL이나 오류 응답 처리를 중복 구현하지 않습니다.
- 공개 API 오류는 `{ "code": string, "message": string }` 형식을 유지합니다.
  데이터 없음은 오류가 아니라 빈 목록을 포함한 성공 응답으로 처리합니다.

## 백엔드 가이드라인

- 공개 함수, 메서드 및 데이터 구조에 명확한 타입을 사용하고 타입 검사 오류를
  우회하기 위한 무분별한 `Any`나 타입 무시는 피합니다.
- FastAPI 경계에서 쿼리, 경로 및 날짜를 검증하고 서비스 계층에서도 유스케이스
  규칙을 검증합니다.
- NEIS 응답은 Pydantic 외부 모델로 검증한 뒤 camelCase 직렬화 별칭을 사용하는
  내부 모델로 변환합니다. 화면에 외부 응답 행을 그대로 반환하지 않습니다.
- NEIS의 HTTP 오류와 HTTP 200 응답 안의 `RESULT` 업무 오류를 모두 검사하고,
  인증 실패, 요청 제한, 시간 초과, 비정상 스키마 및 데이터 없음을 구분합니다.
- 급식 조회는 `MMEAL_SC_CODE=2`인 중식만 반환하고, 메뉴의 `<br>` 구분을 목록으로
  정규화하며, 결과를 날짜 오름차순으로 유지합니다.
- 페이지 크기는 `Settings.neis_page_size`를 사용하고 전체 페이지를 순차 조회하는
  기존 동작을 보존합니다.
- 예외는 가능한 한 구체적으로 처리하고, 오류를 숨기거나 성공처럼 반환하지
  않습니다. 호출자가 대응해야 하는 오류는 충분한 문맥과 함께 전달합니다.
- Python 3.11 이상을 지원하고 의존성은 `src/backend/pyproject.toml`에서
  관리합니다. 필요한 경우에만 추가하고 표준 라이브러리나 기존 의존성을 먼저
  검토합니다.
- 코드 스타일, 포맷팅 및 정적 분석은 저장소에 구성된 도구와 설정을 따릅니다.

## 프론트엔드 가이드라인

- 엄격한 타입을 유지하고 `any`, 불필요한 타입 단언 및 null 가능성 무시를
  피합니다. 외부 데이터는 런타임 검증 후 사용합니다.
- React 함수 컴포넌트와 Fluent UI 구성 요소를 사용하고 기존 접근성 속성,
  키보드 조작 및 한국어 UI 문구를 보존합니다.
- 학교 검색, 날짜 선택, 급식 결과의 3단계 흐름을 유지합니다. 학교나 날짜가
  변경되면 더 이상 유효하지 않은 급식 결과를 초기화합니다.
- 요청 상태는 `idle`, `loading`, `success`, `empty`, `error`를 명시적으로
  구분하고 로딩, 빈 결과, 입력 오류, 서버 오류를 각각 사용자에게 표시합니다.
- 자동 완성은 두 글자 이상에서 300ms 후 요청하며 최대 6개를 표시합니다. 새로운
  검색이나 컴포넌트 정리 시 이전 요청을 `AbortController`로 취소해 오래된 응답이
  최신 상태를 덮어쓰지 않게 합니다.
- 오류 상태를 명시적으로 처리하고 예외나 거부된 Promise를 조용히 삼키지
  않습니다. 사용자에게 필요한 실패 정보는 기존 UI 패턴에 맞게 표시합니다.
- 컴포넌트와 함수는 한 가지 책임에 집중하며, 기존 모듈과 유틸리티를 우선
  재사용합니다.
- Node.js 24 이상과 npm을 사용합니다. 의존성을 변경하면
  `src/frontend/package.json`과 `src/frontend/package-lock.json`을 함께
  갱신합니다.
- 컴파일러, 포맷터, 린터 설정을 우회하지 말고 저장소에 구성된 규칙을 따릅니다.

## 테스트 및 검증

- 변경된 동작에는 정상 경로, 실패 경로 및 중요한 경계 조건을 다루는 테스트를
  추가하거나 수정합니다.
- 프론트엔드 테스트는 Vitest와 Testing Library로 사용자 흐름 및 접근 가능한
  역할을 기준으로 작성하고, 네트워크 경계는 `fetch`를 모킹합니다.
- 백엔드 테스트는 pytest와 HTTPX의 `MockTransport` 또는 `ASGITransport`를
  사용하며 실제 NEIS API나 실제 인증키에 의존하지 않습니다.
- 변경 범위를 검증하는 가장 작은 관련 테스트부터 실행하고, 필요한 경우 전체
  검사로 확장합니다.
- 프론트엔드 전체 검증은 `src/frontend`에서 `npm test`와 `npm run build`를
  실행합니다.
- 백엔드 전체 검증은 `src/backend`에서 `python -m pytest`를 실행합니다.
- Compose 변경은 저장소 루트에서 `docker compose config`로 검증합니다.
- 아직 구성되지 않은 도구를 새로 도입하거나 존재하지 않는 명령을 검증 절차로
  가정하지 않습니다. 실행할 수 없는 검사는 결과에 명확히 기록합니다.
- 테스트를 통과시키기 위해 검사를 삭제하거나 완화하지 않습니다.

## 실행 및 환경 설정

- `.env.example`을 복사해 `.env`를 만들고 `NEIS_API_KEY`를 설정합니다. 실제
  인증키가 없는 테스트에는 안전한 더미 값을 사용합니다.
- Docker 실행은 저장소 루트에서 `docker compose up --build`를 사용하며 기본
  접속 주소는 `http://localhost:8080`입니다.
- 로컬 통합 실행은 Windows에서 `pwsh ./scripts/start.ps1`, macOS/Linux에서
  `bash ./scripts/start.sh`를 사용합니다. 프론트엔드는
  `http://127.0.0.1:5173`, 백엔드는 `http://127.0.0.1:8000`에서 실행됩니다.
- 백엔드의 `/health` 엔드포인트와 Compose의 서비스 준비 상태 의존성을
  보존합니다.

## 보안

- API 키, 토큰, 암호, 개인정보 및 기타 민감한 데이터를 코드, 로그, 테스트
  픽스처 또는 커밋에 포함하지 않습니다.
- `NEIS_API_KEY`는 백엔드 환경 변수로만 주입하고 전체 요청 URL, 쿼리의 `Key`
  값이나 민감한 헤더를 로그에 남기지 않습니다. 예제에는 안전한 플레이스홀더만
  둡니다.
- 사용자 입력과 외부 응답을 검증하고, 출력 인코딩, 최소 권한 및 안전한 기본값을
  적용합니다.
- 보안 취약점은 공개 이슈에 기록하지 않고 `SECURITY.md`의 비공개 신고 절차를
  따릅니다.

## 문서화

- 동작, 설정, 공개 API 또는 개발 절차가 바뀌면 관련 README, 워크숍 문서,
  API 명세 및 예제를 함께 갱신합니다.
- 문서는 현재 구현과 일치해야 하며 검증하지 않은 명령이나 기능을 완료된 것처럼
  설명하지 않습니다.
- 기존 문서의 언어, 용어, 링크 형식 및 구조를 유지합니다.

## 주의사항 및 가드레일

- 사용자 변경사항과 관련 없는 파일을 되돌리거나 덮어쓰지 않습니다.
- 명시적인 요청 없이 파괴적인 Git 명령, 대규모 파일 삭제, 데이터 마이그레이션
  또는 호환성을 깨는 변경을 수행하지 않습니다.
- API 명세와 외부 서비스의 오류 형식을 포함한 계약을 확인하고 추측으로 필드나
  동작을 만들지 않습니다.
- 오류를 무시하는 넓은 예외 처리, 무조건적인 기본값 및 실패를 성공으로 위장하는
  폴백을 추가하지 않습니다.
- 범위나 요구사항이 충돌하고 결과에 큰 영향을 주는 경우 임의로 결정하지 말고
  먼저 명확히 확인합니다.

## Git 커밋 및 Pull Request

- 커밋은 하나의 논리적 변경에 집중하고
  [Conventional Commits](https://www.conventionalcommits.org/) 형식을 따릅니다.
- 관련 없는 변경이나 생성 산출물을 커밋에 포함하지 않으며 비밀정보가 없는지
  확인합니다.
- Pull Request는 하나의 목적에 집중하고 변경 이유, 주요 변경사항, 검증 결과와
  영향을 설명합니다.
- Pull Request를 작성할 때는 `.github/PULL_REQUEST_TEMPLATE.md`를 반드시
  활용하고 해당하는 항목을 빠짐없이 작성하며, `Closes #<번호>` 형식으로 관련
  이슈를 연결합니다.
- 검토 가능한 크기를 유지하고 관련 테스트와 문서를 포함하며, 구성된 CI 검사가
  통과하도록 합니다.
