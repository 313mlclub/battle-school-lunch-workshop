# AGENTS.md

## 프로젝트 개요

이 저장소는 NEIS 공개 API를 활용해 초중고 급식 메뉴를 조회하고 분석하는
웹 애플리케이션을 단계적으로 구현하는 워크숍입니다. 현재 저장소에는 워크숍
문서와 API 명세가 있으며, 프론트엔드, 백엔드, 배포 및 에이전트 관련 코드는
각 단계의 요구사항에 따라 추가됩니다.

## 일반 작업 지침

- 요청된 범위와 관련 파일을 먼저 확인하고 변경을 해당 범위로 제한합니다.
- 이 문서보다 구체적인 저장소 문서, 디렉터리별 지침, 설정 파일 및 기존 코드
  패턴이 있으면 이를 우선합니다.
- 기존 동작과 공개 인터페이스를 보존하며, 동작 변경이 필요하면 테스트와
  문서를 함께 갱신합니다.
- 현재 요구사항에 필요하지 않은 리팩터링, 새로운 계층, 범용화 및 추상화를
  추가하지 않습니다.
- 이미 사용 중인 헬퍼와 패턴을 재사용하고 중복 구현을 피합니다.
- 생성 파일이나 외부 명세를 수정할 때는 원본 및 생성 절차를 먼저 확인합니다.

## Python 가이드라인

- 공개 함수, 메서드 및 데이터 구조에 명확한 타입을 사용하고 타입 검사 오류를
  우회하기 위한 무분별한 `Any`나 타입 무시는 피합니다.
- 예외는 가능한 한 구체적으로 처리하고, 오류를 숨기거나 성공처럼 반환하지
  않습니다. 호출자가 대응해야 하는 오류는 충분한 문맥과 함께 전달합니다.
- 입력값과 외부 API 응답을 신뢰하지 말고 경계에서 검증합니다.
- 의존성은 기존 Python 프로젝트 설정과 잠금 방식을 따릅니다. 필요한 경우에만
  추가하고, 표준 라이브러리나 기존 의존성으로 해결할 수 있는지 먼저 확인합니다.
- 코드 스타일, 포맷팅 및 정적 분석은 저장소에 구성된 도구와 설정을 따릅니다.

## TypeScript 가이드라인

- 엄격한 타입을 유지하고 `any`, 불필요한 타입 단언 및 null 가능성 무시를
  피합니다. 외부 데이터는 런타임 검증 후 사용합니다.
- 오류 상태를 명시적으로 처리하고 예외나 거부된 Promise를 조용히 삼키지
  않습니다. 사용자에게 필요한 실패 정보는 기존 UI 패턴에 맞게 표시합니다.
- 컴포넌트와 함수는 한 가지 책임에 집중하며, 기존 모듈과 유틸리티를 우선
  재사용합니다.
- 의존성은 기존 패키지 관리자와 잠금 파일을 사용합니다. 필요한 경우에만
  추가하고 버전 및 잠금 파일을 일관되게 갱신합니다.
- 컴파일러, 포맷터, 린터 설정을 우회하지 말고 저장소에 구성된 규칙을 따릅니다.

## 테스트 및 검증

- 변경된 동작에는 정상 경로, 실패 경로 및 중요한 경계 조건을 다루는 테스트를
  추가하거나 수정합니다.
- 변경 범위를 검증하는 가장 작은 관련 테스트부터 실행하고, 필요한 경우 전체
  검사로 확장합니다.
- 저장소에 실제로 구성된 스크립트와 설정을 확인한 뒤 테스트, 포맷팅, 린트,
  타입 검사 및 빌드를 실행합니다.
- 아직 구성되지 않은 도구를 새로 도입하거나 존재하지 않는 명령을 검증 절차로
  가정하지 않습니다. 실행할 수 없는 검사는 결과에 명확히 기록합니다.
- 테스트를 통과시키기 위해 검사를 삭제하거나 완화하지 않습니다.

## 보안

- API 키, 토큰, 암호, 개인정보 및 기타 민감한 데이터를 코드, 로그, 테스트
  픽스처 또는 커밋에 포함하지 않습니다.
- 비밀값은 환경 변수나 프로젝트에서 정한 비밀 관리 방식을 사용하며 예제에는
  안전한 플레이스홀더만 둡니다.
- 사용자 입력과 외부 응답을 검증하고, 출력 인코딩, 최소 권한 및 안전한 기본값을
  적용합니다.
- 보안 취약점은 공개 이슈에 기록하지 않고 `SECURITY.md`의 비공개 신고 절차를
  따릅니다.

## 문서화

- 동작, 설정, 공개 API 또는 개발 절차가 바뀌면 관련 README, 워크숍 문서,
  API 명세 및 예제를 함께 갱신합니다.
- 문서는 현재 구현과 일치해야 하며 검증하지 않은 명령이나 기능을 완료된 것처럼
  설명하지 않습니다.
- 기존 문서의 언어, 용어, 링크 형식 및 구조를 유지합니다.

## 주의사항 및 가드레일

- 사용자 변경사항과 관련 없는 파일을 되돌리거나 덮어쓰지 않습니다.
- 명시적인 요청 없이 파괴적인 Git 명령, 대규모 파일 삭제, 데이터 마이그레이션
  또는 호환성을 깨는 변경을 수행하지 않습니다.
- API 명세와 외부 서비스의 오류 형식을 포함한 계약을 확인하고 추측으로 필드나
  동작을 만들지 않습니다.
- 오류를 무시하는 넓은 예외 처리, 무조건적인 기본값 및 실패를 성공으로 위장하는
  폴백을 추가하지 않습니다.
- 범위나 요구사항이 충돌하고 결과에 큰 영향을 주는 경우 임의로 결정하지 말고
  먼저 명확히 확인합니다.

## Git 커밋 및 Pull Request

- 커밋은 하나의 논리적 변경에 집중하고
  [Conventional Commits](https://www.conventionalcommits.org/) 형식을 따릅니다.
- 관련 없는 변경이나 생성 산출물을 커밋에 포함하지 않으며 비밀정보가 없는지
  확인합니다.
- Pull Request는 하나의 목적에 집중하고 변경 이유, 주요 변경사항, 검증 결과와
  영향을 설명합니다.
- Pull Request를 작성할 때는 `.github/PULL_REQUEST_TEMPLATE.md`를 반드시
  활용하고 해당하는 항목을 빠짐없이 작성하며, `Closes #<번호>` 형식으로 관련
  이슈를 연결합니다.
- 검토 가능한 크기를 유지하고 관련 테스트와 문서를 포함하며, 구성된 CI 검사가
  통과하도록 합니다.
